source :rubygems

gem 'guard'
gem 'guard-coffeescript'

# Make sure to also install the proper browswer extension
# See docs: https://github.com/guard/guard-livereload
gem 'guard-livereload'
gem 'yajl-ruby'

gem 'growl_notify'

# Only on OSX
gem 'rb-fsevent' if RUBY_PLATFORM =~ /darwin/
